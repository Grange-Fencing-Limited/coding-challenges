### Task 1

Create a database and a simple table to store users.

**Requirements:**

- Create a database named `test_db`
- Create a `users` table with `id` (primary key, auto-increment), `name` (varchar), `email` (varchar)

---

### Task 2

Insert rows and run basic SELECT queries.

**Requirements:**

- Insert 2–3 rows into `users`
- Select all rows
- Select with a WHERE clause

---

### Task 3

Update and delete rows.

**Requirements:**

- Update a user's name by id
- Delete a user by id

---

### Task 4

Simple INNER JOIN between two tables.

**Requirements:**

- Create a second table `orders` with `id`, `user_id`, `amount`
- Insert a couple of orders referencing `users.id`
- Query orders joined with user names

---